railsを動作させる環境が整っていれば、以下の手順で動作可能な筈です。
(制作したのが１年以上前なのと、環境構築は先生の指示の下で行ったので大雑把な内容になります)

1.適当なファイル(名前例:rails_begi　←これを以下の名称とする)を用意し、ここgithubのbegitaberu内にあるデータをすべて入れる
2.データベースにある各種データを渡したいのでコマンド[rake db:migrate:reset]を実行する
3.ターミナルでrails_begiの階層まで移動し、コマンド[rails s]でサーバー起動
4.ブラウザを立ち上げ、URLに「http://localhost:3000/」を入力






基本的にはappファイルの中に、私が書いたコードなどのデータが詰まっています。



