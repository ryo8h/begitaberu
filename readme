railsを動作させる環境が整っていれば、以下の手順で動作可能な筈です。
(制作したのが１年以上前なのと、環境構築は先生の指示の下で行ったので大雑把な内容になります)

1.適当なファイル(名前例:rails_begi　←これを以下の名称とする)を用意する。ターミナルでコマンド[rails new rails_begi]を実行するやり方で。
2.rails_begiに、ここgithubのbegitaberu内にあるデータをすべて入れる(スクリーンショットフォルダとreadmeは入れなくて良い)
　　　　（rails newによって様々なファイルが一緒に作成されており、appなど同じファイル名が存在するが、上書きで問題ない）
3.データベースにある各種データを渡したいのでコマンド[rake db:migrate:reset]を実行する
4.ターミナルでrails_begiの階層まで移動し、コマンド[rails s]でサーバー起動
5.ブラウザを立ち上げ、URLに「http://localhost:3000/」を入力






基本的にはappファイルの中に、私が書いたコードなどのデータが詰まっています。



